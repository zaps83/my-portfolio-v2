import React from 'react'
import * as S from './styles/string'

const String = ({ page }) => {

    return (
            <S.Outer page={page}>
                "..."
            </S.Outer>

    )
}

export default String